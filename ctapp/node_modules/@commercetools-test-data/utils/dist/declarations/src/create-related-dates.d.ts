import type { Faker } from '@faker-js/faker';
type TGetDate = (fake: Faker) => string;
declare const createRelatedDates: (recentDaysFromPastReference?: number) => [olderDate: TGetDate, newerDate: TGetDate, futureDate: TGetDate];
export default createRelatedDates;
