'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/* eslint-disable jest/no-conditional-expect */

function createBuilderSpec(transformType, builder, expected) {
  return ["should build properties for \"".concat(transformType, "\""), () => {
    switch (transformType) {
      case 'default':
        {
          const defaultModel = builder.build();
          expect(defaultModel).toEqual(expected);
          break;
        }
      case 'rest':
        {
          const defaultModel = builder.buildRest();
          expect(defaultModel).toEqual(expected);
          break;
        }
      case 'graphql':
        {
          const defaultModel = builder.buildGraphql();
          expect(defaultModel).toEqual(expected);
          break;
        }
      default:
        throw new Error("Unknown transform type ".concat(transformType, "."));
    }
  }];
}

exports.createBuilderSpec = createBuilderSpec;
